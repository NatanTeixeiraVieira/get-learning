import { Loader2 } from 'lucide-react';

import { IconSpin } from './styles';

export default function ButtonIconSpin() {
  return (
    <IconSpin>
      <Loader2 strokeWidth={2} />
    </IconSpin>
  );
}
